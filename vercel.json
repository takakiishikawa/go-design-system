{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm ci && npm run build && npm ci --prefix showcase && npm run build --prefix showcase",
  "outputDirectory": "showcase/out",
  "git": {
    "deploymentEnabled": {
      "main": true,
      "**": false
    }
  }
}
