The fix: replaced the conditional expression that passed `tooltipLabelFormatter` directly as `labelFormatter` with a single lambda `(label) => tooltipLabelFormatter ? tooltipLabelFormatter(label as string) : xTickFormatter(label as string)`. This gives TypeScript a function whose signature (`(label: ReactNode) => ReactNode`) is compatible with what `ChartTooltipContent.labelFormatter` expects, while still delegating to the user-supplied formatter when provided.